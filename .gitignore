.venv/
state.db
screens/
config.toml
__pycache__/
*.log
.pytest_cache/
spike/ws_frames.jsonl
state.db-wal
state.db-shm
watch_out.txt
watch_err.txt
ui_state.json
*.bak.db
*.bak.toml
build/
dist/
givvy/_build.py
logs/
recordings/
# what --report, --trial-report, --check-reader and --check-update write beside the program: account names, sellers
# and prize titles, which installer/release.py's 'git add -A' would otherwise commit
results.txt
trial_report.txt
reader_check.txt
reader_check_hang.txt
update_check.txt

# theme art that is not ours to publish (e.g. Riot's Nunu & Willump), kept on each PC only
themes/
givvy.log.*
